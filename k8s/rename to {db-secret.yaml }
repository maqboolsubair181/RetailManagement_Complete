apiVersion: v1
kind: Secret
metadata:
  name: retail-db-secret
type: Opaque
stringData:
  # Replace these values with your actual RDS / MySQL endpoint and credentials
  # before applying to the cluster.
  # DO NOT commit real credentials to version control.
  # Use AWS Secrets Manager or Sealed Secrets in production.
  DB_URL: "jdbc:mysql://mysql-2870f1bd-makkusubair181-1166.e.aivencloud.com:14393/defaultdb?sslMode=REQUIRED&requireSSL=true"
  DB_USER: "avnadmin"
  DB_PASS: "AVNS_Z0iU8OORJnyg4Yra6HI"
